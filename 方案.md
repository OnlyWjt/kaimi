# Kaimi 开源方案

> 版本：0.1  
> 日期：2026-08-25  
> 状态：一期骨架已落地（当前仓库），待联调主站

Kaimi（开密）是发给**二级代理自托管**的开源白标系统。  
你已有 CDK 主站；本仓库不替代主站，只做代理自己的门面。

上游唯一接口：主站 Agent 开放 API（`Authorization: Bearer ak_live_…`），即仓库外那份 `agent-api-zh` 文档。

---

## 1. 定位

主站负责货和开通；Kaimi 负责代理自己的店。

| 谁 | 手里有什么 | 干什么 |
| --- | --- | --- |
| 你（CDK 主站） | CDK 系统、卡台、代理 API Key、webhook 密钥 | 发卡给代理、开通、对账；把 Kaimi 交给代理部署 |
| 二级代理 | 一份 Kaimi、主站 API Key、自己的易支付 | 换皮、上架发卡网、卖代充、自己收客户的钱 |
| 终端客户 | 只看见代理的站 | 在发卡网买码，或在代充店提交 session / 邮箱 |

明确不做：

- 再造卡台 / 再造你的 CDK 主站
- 把主站 API Key 下发到浏览器
- 一期接独角、异次元等第三方发卡程序（代理要的是自己的发卡网）
- 一期上 K8s、独立消息队列

仓库名：`kaimi`。不叫 CDK-xxx，避免和 AWS CDK、市面发卡站撞名。

备选名：Lumen、Relay。

---

## 2. 系统怎么叠

```
终端客户 ──► 发卡网（代理独立品牌）
           └► 代充店（代理独立品牌）
二级代理 ──► 两套店的后台（品牌 / 商品 / 库存 / 进货）
                    │
                    ▼
              Kaimi 引擎（库存 · 订单 · 回调）
                    │  Bearer ak_live_
                    ▼
              你的 CDK 主站（Agent API）
```

出网只有一件事：代理服务器用 API Key 调你的主站。  
发卡网和代充店可以同域名不同路径，也可以两个域名两套皮。

---

## 3. 两套店面

引擎一份，皮两套。各有一份 `storefront_settings`，字段结构相同、值可以不同。

| 字段 | 发卡网 | 代充店 |
| --- | --- | --- |
| site_name / logo / favicon | 代理自己的发卡品牌 | 代理自己的代充品牌 |
| theme_id / accent | 可与代充店不同 | 可与发卡网不同 |
| domain | `card.xxx.com` 或 `/shop` | `cdk.xxx.com` 或 `/recharge` |
| announcement / contacts / ICP | 要 | 要 |
| home banner / 商品文案 | 要（发卡网核心） | 套餐说明即可 |
| 售后文案 | 发卡须知 | 代充须知、风险提示 |
| 开关 | 只开发卡 / 只开代充 / 两个都开 | 同左 |

### 3.1 主题（一套布局，换 token）

不 fork 多套 HTML。全部走 CSS 变量。

| 主题 id | 观感 | 适合 |
| --- | --- | --- |
| aurora | 深色科技，默认 | 多数 GPT 店 |
| snow | 浅色干净 | 正价、账单可查 |
| ink | 黑白极简 | 偏工具 |
| sakura | 浅色偏柔 | 和赛博风拉开 |

二期再加社区主题包（如 neon / forest），不进核心。

---

## 4. 发卡网（代理自己的店）

发卡网是 Kaimi **内置**能力，不是你的主站前台。  
客户买到的是代理库存里的码，不会回到主站公开兑换页（主站对该渠道返回 `CDK_AGENT_CHANNEL`）。

### 4.1 两种商品

| 类型 | 客户付完款之后 | 是否调主站开通 |
| --- | --- | --- |
| 售码 | 立刻看到卡密 | 否。只从本地库存扣一张未使用码 |
| 代充 SKU | 跳转填 session / 邮箱 | 是。锁码后 `POST /agent/recharge`，客户始终看不到完整码 |

### 4.2 代理能自定义什么

| 块 | 可改内容 | 一期 |
| --- | --- | --- |
| 站点 | 站名、Logo、favicon、主题、主色、域名 | 要 |
| 首页 | 公告、banner、分类排序、是否显示库存 | 要 |
| 商品 | 标题、封面、详情 HTML、售价、关联主站 plan | 要 |
| 下单 | 邮箱必填、数量上限、支付方式文案 | 要 |
| 交卡 | 卡密页模板、复制按钮、使用说明 | 要 |
| 查单 | 订单号 / 邮箱查卡密（售码）或开通进度（代充） | 要 |
| 售后 | 售后规则、联系方式、页脚 / ICP | 要 |
| 开关 | 只开发卡网、只开代充、两个都开 | 要 |

### 4.3 公开页

| 路径 | 谁用 | 做什么 |
| --- | --- | --- |
| `/shop` | 客户 | 发卡网首页、分类、商品 |
| `/shop/item/:id` | 客户 | 商品详情、下单 |
| `/shop/order/:no` | 客户 | 支付结果、展示已售卡密 |
| `/shop/query` | 客户 | 邮箱 / 订单号查卡 |
| `/recharge` | 客户 | 代充提交凭据 |
| `/lookup` | 客户 | 开通进度 |
| `/admin` | 二级代理 | 品牌、商品、库存、进货、订单 |
| `/api/webhook` | 主站 | 验签后更新本地开通单 |

### 4.4 库存互斥

同一张码不能同时挂在待支付发卡单和在途代充单上。

| 动作 | 库存变化 | 客户看不看得到码 |
| --- | --- | --- |
| 发卡网售出 | unused → sold，绑定发卡订单 | 支付成功后展示 |
| 代充占用 | unused → locked → used（终态成功） | 不展示 |
| 代充失败可释放 | locked → unused（failed 且未消耗） | 不展示 |

售出的码不再拿去调开通。若业务是「客户买码后再自己来代充页填码」，那是客户持码，不是店库存。

---

## 5. 上游对接（你的 Agent API）

鉴权一律：

```
Authorization: Bearer ak_live_xxxxxxxxxxxx
```

Key 只存在代理服务器。充值异步：`202` 之后优先 webhook；webhook 不可达时再按 ≥5 秒轮询。批量请求带 `Idempotency-Key`。

| Kaimi 动作 | 主站接口 | 注意 |
| --- | --- | --- |
| 拉可售套餐，给发卡网 / 代充做商品映射 | `GET /agent/plans` | 不要写死 plus / pro |
| 同步代理库存 | `GET /agent/cdks` | 完整码只在服务端；售码页加密下发一次 |
| 代理在线进货 | `POST /agent/orders` | `pay_url` 只出现在代理后台 |
| 进货单查询 / 续付 | `GET /agent/orders/{order_no}`、`POST .../repay` | 待支付 30 分钟 |
| 代充提交 | `POST /agent/recharge` | 必须带 `cdk_code` |
| 批量代充 | `POST /agent/batch-recharge` | 一期可只做单条 |
| 查开通 | `GET /agent/recharge/{request_id}` | webhook 失败时的兜底 |
| 代理对账 | `GET /agent/records` | session 检索走 `POST /agent/records/search-session` |
| 回调排障 | `/agent/webhooks/deliveries` | 代理后台可见 |

### 5.1 Webhook 硬规则

- 验签：`hex(HMAC-SHA256(secret, timestamp + "." + rawBody))`，必须用**原始字节**
- 时间窗：`X-Webhook-Timestamp` 与当前时间相差不超过 5 分钟
- 幂等：按 `event_id` / `X-Webhook-Id` 去重（同一事件可能投递多次）
- 返回 2xx 视为成功；否则主站按 30s / 2m / 10m / 30m / 2h 重试，最多 6 次

### 5.2 开通状态机

| 上游状态 | 代充店展示 | 能不能重提同一账号 |
| --- | --- | --- |
| pending / issuing / preparing / submitted / processing | 处理中 | 否 |
| success | 已开通 | 否 |
| failed | 失败，可换码重试 | 可以，须换新卡密 |
| skipped | 已是目标套餐，未扣码 | 否 |
| unknown | 结果不确定，人工核 | **绝对不要** |

`unknown` 必须锁单，禁止再 `POST /agent/recharge`。上游可能已扣款。

遇到 `429`、`502` 可以安全重试；程序分支判断 `error_code`，不要依赖 `error` 文案。

---

## 6. 两层支付，不要混

| 层 | 谁付钱 | 走哪套易支付 | 对应接口 |
| --- | --- | --- | --- |
| 进货 | 代理 → 你的主站 | 主站已配好的易支付 | `POST /agent/orders` |
| 零售 | 客户 → 二级代理 | Kaimi 里代理自己配的易支付（可以不是同一商户） | Kaimi 本地订单，不调主站支付 |

客户在子店付款不能复用主站 `pay_url`，否则钱进错账户，加价和发卡都会对不上。

一期若代理还没配易支付，可先做「店主已收款，客户只提交代充 / 后台手动发卡」。

---

## 7. 数据模型（一期）

| 表 | 存什么 |
| --- | --- |
| settings | 主站 URL / Key、webhook secret、代理易支付 |
| storefronts | 发卡网 / 代充店两套品牌与开关 |
| products | 发卡商品：文案、售价、关联 plan、类型（售码或代充） |
| plans_cache | 主站套餐 + 代理加价 |
| cdk_pool | 库存、锁定、售出、绑定哪张本地单 |
| orders | 客户单（发卡或代充）、支付态、`request_id`、`client_reference` |
| webhook_events | `event_id` 幂等去重 |
| admin_users | 二级代理登录 |

凭据（session、邮箱密码）仅用于提交开通：不写访问日志、不回显、排队时加密；session 按主站约定只比对哈希。

---

## 8. 技术选型

二级代理要能自己部署，优先一个容器能跑。

- **应用**：Next.js 15 App Router（发卡网 + 代充店 + 代理后台 + BFF + webhook）
- **数据**：SQLite 默认；Postgres 用环境变量切换
- **ORM**：Drizzle
- **上游 SDK**：`packages/upstream`，类型化客户端，API Key 只留服务端
- **主题**：`packages/themes`，CSS 变量
- **部署**：Docker Compose + Caddy，一条命令起站

### 仓库结构

```
apps/web                 发卡网、代充店、代理后台、Webhook、BFF
packages/upstream        鉴权、幂等键、轮询、错误码映射
packages/themes          aurora / snow / ink / sakura
deploy/                  compose、Caddy、给代理的一键脚本
.env.example             KAIMI_UPSTREAM_BASE_URL / API_KEY / WEBHOOK_SECRET
```

---

## 9. 分期

目标用户从第一天就是二级代理。所谓「二期」是代理再招下级（三级），不是现在这层。

### 一期 · 代理能上线

- 一键部署，向导填主站 URL + API Key
- 可换皮发卡网：上架、售码、查卡
- 可换皮代充店：付款、提交凭据、查单
- 进货 / 库存同步 / webhook
- 客户侧易支付（或先做已收款通道）

### 二期 · 代理再招人

- 三级子账户、划卡、加价
- 子店独立发卡网皮
- Kaimi 自己发给下级的 API Key
- 下级不碰主站 `ak_live_`

### 三期 · 生态

- 主题包
- 批量开通、对账导出
- Telegram 通知
- 独角等旧发卡网导入（可选）

### 一期完成标准

| 场景 | 通过条件 |
| --- | --- |
| 代理开站 | `compose up` 后向导填主站 URL + API Key 即可卖 |
| 换发卡网皮 | 改发卡网站名 / Logo / 主题，`/shop` 立即变；代充店可保持另一套 |
| 售码 | 客户付款后看到卡密，库存 unused → sold，主站未调 recharge |
| 代充 | 锁码 → 202 → webhook 终态 → 查单更新，客户看不到码 |
| unknown | 锁单且不再 POST |
| 安全 | 浏览器看不到 `ak_live_`；未支付订单看不到完整码 |

---

## 10. 安全清单

- 主站 API Key、webhook secret 加密落库，永不进前端包
- Webhook 原始字节验签 + 5 分钟时间窗 + `event_id` 幂等
- 所有代充带 `Idempotency-Key`（UUID）
- 卡密完整码：售码仅在已支付订单页展示一次；代充路径永不展示
- 凭据不进日志、不进异常堆栈
- 遵守主站限额：`rate_limit_rpm`、`max_concurrent_recharge`、`max_batch_items`
- `unknown` / 在途单禁止对同一账号重复提交

---

## 11. 待拍板（开工前）

1. 产品名确认用 **Kaimi**，还是换成 Lumen / Relay / 自定。
2. 一期客户付款是否上易支付；不上则先做「已收款，仅提交 / 后台发卡」。
3. 仓库路径已定为 `C:\Users\onlyWjt\kaimi`。
